import { Meta, StoryObj } from '@storybook/react';
import { BasicFooter } from '.';

export default {
	component: BasicFooter,
	args: {},
} as Meta<typeof BasicFooter>;

type Story = StoryObj<typeof BasicFooter>;

export const Default: Story = {};
